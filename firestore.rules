rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Progreso de Sofía (avatar zorro). App familiar: sin login; restringe solo este documento.
    match /players/{playerId} {
      allow read, write: if playerId == "zorro";
    }
  }
}
